apiVersion: v2
name: inference
description: The prediction API of the California Housing model, rolled out as a canary.
type: application

# The chart version changes when the templates change. The model version is not
# part of it: the model is chosen in gitops/envs/*.yaml, not here.
version: 0.1.0
appVersion: "1.0.0"

keywords:
  - mlops
  - inference
  - canary
home: https://github.com/MaksDeGreez/goit-mlops
